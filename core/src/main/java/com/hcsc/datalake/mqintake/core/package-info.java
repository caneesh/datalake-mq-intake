/**
 * Core module for datalake-mq-intake.
 *
 * Contains: transactional receive loop, batching, degraded mode,
 * sequence file writer, _tmp staging + rename, path stamping, audit records,
 * reconciliation, Kerberos handling, RecordSerializer interface.
 */
package com.hcsc.datalake.mqintake.core;
